// import { fromJS } from 'immutable';
// import { selectMessagesListPageDomain } from '../selectors';

describe('selectMessagesListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
