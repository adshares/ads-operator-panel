// import { fromJS } from 'immutable';
// import { selectNodesListPageDomain } from '../selectors';

describe('selectNodesListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
