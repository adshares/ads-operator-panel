// import { fromJS } from 'immutable';
// import { selectAccountsListPageDomain } from '../selectors';

describe('selectAccountsListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
