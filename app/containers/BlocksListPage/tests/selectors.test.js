// import { fromJS } from 'immutable';
// import { selectBlocksListPageDomain } from '../selectors';

describe('selectBlocksListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
