// import { fromJS } from 'immutable';
// import { selectTransactionsListPageDomain } from '../selectors';

describe('selectTransactionsListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
