// import { fromJS } from 'immutable';
// import { selectSnapshotsListPageDomain } from '../selectors';

describe('selectSnapshotsListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
