// import { fromJS } from 'immutable';
// import { selectSnapshotPageDomain } from '../selectors';

describe('selectSnapshotPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
