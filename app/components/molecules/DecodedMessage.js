import styled from 'styled-components';

export const DecodedMessage = styled.pre`
  margin: 0;
  font-size: 87.5%;
  white-space: pre-wrap;
  word-break: break-word;
`;

DecodedMessage.displayName = 'DecodedMessage';
